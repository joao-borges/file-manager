# File Manager - Full Stack Architecture

## System Overview

```
┌────────────────────────────────────────────────────────────────┐
│                         User Browser                           │
│                      http://localhost:8080                     │
└───────────────────────────┬────────────────────────────────────┘
                            │
                            │ HTTP
                            │
┌───────────────────────────▼────────────────────────────────────┐
│                     Spring Boot 3.5.8                          │
│                      (Java 21 Backend)                         │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Static Resource Handler                    │  │
│  │         (Serves React App from /static)                 │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                            │                                    │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │          FileOperationsController                       │  │
│  │            (REST API Endpoints)                         │  │
│  │                                                          │  │
│  │  POST /api/operations/rename                            │  │
│  │  POST /api/operations/organize                          │  │
│  │  POST /api/operations/extract                           │  │
│  │  POST /api/operations/photo-organize                    │  │
│  │  POST /api/operations/find-duplicates                   │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                            │                                    │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │              Operation Services Layer                   │  │
│  │                                                          │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │  │
│  │  │  Renomeador  │  │ Organizador  │  │   Extrator   │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  │  │
│  │                                                          │  │
│  │  ┌──────────────┐  ┌──────────────────────────────────┐ │  │
│  │  │PhotoOrganizat│  │    DuplicateFinder               │ │  │
│  │  └──────────────┘  └──────────────────────────────────┘ │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                            │                                    │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │              File System Operations                     │  │
│  │        (Read, Write, Move, Delete files)                │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Frontend Architecture (React + TypeScript)

```
┌────────────────────────────────────────────────────────────────┐
│                      React Application                         │
│                   (TypeScript 5.7 + Vite 6)                    │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                      App.tsx                            │  │
│  │              (Main Router & Layout)                     │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                            │                                    │
│       ┌────────────────────┼────────────────────┐              │
│       │                    │                    │              │
│  ┌────▼──────┐   ┌────────▼─────┐   ┌─────────▼──────┐       │
│  │  Rename   │   │  Organize    │   │   Extract      │       │
│  │ Operation │   │  Operation   │   │  Operation     │       │
│  └───────────┘   └──────────────┘   └────────────────┘       │
│                                                                 │
│  ┌─────────────┐   ┌────────────────────────────────┐         │
│  │   Photo     │   │     Duplicate Finder           │         │
│  │ Organization│   │      Operation                 │         │
│  └─────────────┘   └────────────────────────────────┘         │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                   Type System                           │  │
│  │                                                          │  │
│  │  ┌────────────────┐         ┌────────────────────────┐ │  │
│  │  │  api.types.ts  │         │  operations.types.ts   │ │  │
│  │  │                │         │                        │ │  │
│  │  │ - ApiResponse  │         │ - OperationId          │ │  │
│  │  │ - ApiError     │         │ - Operation            │ │  │
│  │  │ - *Request     │         │ - OperationStatus      │ │  │
│  │  │ - *Response    │         │ - FormFieldProps       │ │  │
│  │  └────────────────┘         └────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                  API Service Layer                      │  │
│  │                     (api.ts)                            │  │
│  │                                                          │  │
│  │  - Axios HTTP Client (Typed)                            │  │
│  │  - Request/Response Interceptors                        │  │
│  │  - Error Handling                                       │  │
│  │  - Type-Safe API Methods                                │  │
│  └────────────────────────┬────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────┘
                            │ HTTP/JSON
                            │
                     ┌──────▼──────┐
                     │   Backend   │
                     │  REST API   │
                     └─────────────┘
```

## Data Flow - Example: Rename Operation

```
┌──────────────┐
│    User      │
│   Browser    │
└──────┬───────┘
       │ 1. User fills form
       │    - sourceDirectory: "/photos"
       │    - includeSubDirectories: true
       │
┌──────▼────────────────────────────────────────────────┐
│             RenameOperation.tsx                       │
│  - TypeScript validates form inputs                  │
│  - State: loading, result, error                     │
└──────┬────────────────────────────────────────────────┘
       │ 2. Call API service
       │    renameFiles({ sourceDirectory, ... })
       │
┌──────▼────────────────────────────────────────────────┐
│                  api.ts                               │
│  - Type: RenameRequest                                │
│  - Axios POST /api/operations/rename                  │
│  - Return type: Promise<RenameResponse>               │
└──────┬────────────────────────────────────────────────┘
       │ 3. HTTP POST (JSON)
       │
┌──────▼────────────────────────────────────────────────┐
│      FileOperationsController.java                    │
│  @PostMapping("/rename")                              │
│  - Receive RenameRequest DTO                          │
│  - Validate parameters                                │
└──────┬────────────────────────────────────────────────┘
       │ 4. Delegate to service
       │
┌──────▼────────────────────────────────────────────────┐
│              Renomeador.java                          │
│  - Load exclusion rules from exclusions.xml           │
│  - Scan directory for files                           │
│  - Apply renaming logic                               │
│  - Execute file system operations                     │
└──────┬────────────────────────────────────────────────┘
       │ 5. Return result
       │
┌──────▼────────────────────────────────────────────────┐
│      FileOperationsController.java                    │
│  - Wrap result in ResponseEntity                      │
│  - Return JSON: { success, message, filesRenamed }    │
└──────┬────────────────────────────────────────────────┘
       │ 6. HTTP Response (JSON)
       │
┌──────▼────────────────────────────────────────────────┐
│                  api.ts                               │
│  - Axios interceptor handles response                 │
│  - Type: RenameResponse                               │
│  - Return typed data to component                     │
└──────┬────────────────────────────────────────────────┘
       │ 7. Update UI
       │
┌──────▼────────────────────────────────────────────────┐
│             RenameOperation.tsx                       │
│  - setResult(data)                                    │
│  - setLoading(false)                                  │
│  - Display success message                            │
│  - Show files renamed count                           │
└───────────────────────────────────────────────────────┘
       │
┌──────▼───────┐
│    User      │
│   Sees       │
│   Result     │
└──────────────┘
```

## Type Safety Flow

```
TypeScript Source (.tsx)
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
    tsc (Check)      esbuild (Transpile)
         │                 │
         │                 ▼
         │            JavaScript (.js)
         │                 │
         │                 ▼
         │            Vite/Rollup (Bundle)
         │                 │
         │                 ▼
         │         Optimized Bundle
         │                 │
         │                 ▼
         │         Spring Boot /static
         │                 │
         ▼                 ▼
   Type Errors?    Browser (Runtime)
   Fix & Rebuild
```

## Build Pipeline

```
Maven Build (./mvnw clean package)
    │
    ├─────────────────┬─────────────────┐
    │                 │                 │
    ▼                 ▼                 ▼
Backend Build   Node/npm Install  Frontend Build
(Java compile)  (Dependencies)   (npm run build)
    │                 │                 │
    │                 └────────┬────────┘
    │                          │
    │                          ▼
    │                    tsc (Type check)
    │                          │
    │                          ▼
    │                    Vite build
    │                          │
    │                          ▼
    │              ../resources/static/
    │                          │
    └──────────────┬───────────┘
                   │
                   ▼
           Package Everything
                   │
                   ▼
    target/file-manager-4.0-SNAPSHOT.jar
```

## Directory Structure (Detailed)

```
file-manager/
│
├── src/main/
│   ├── java/br/com/joaoborges/filemanager/
│   │   ├── app/
│   │   │   └── FileManager.java                    # Main Spring Boot class
│   │   │
│   │   ├── controller/
│   │   │   └── FileOperationsController.java       # REST API endpoints
│   │   │
│   │   ├── operations/                             # File operations
│   │   │   ├── interfaces/
│   │   │   │   └── FileOperation.java              # Operation interface
│   │   │   ├── renaming/
│   │   │   │   ├── Renomeador.java                 # Rename service
│   │   │   │   ├── RenamingParamsBuilder.java
│   │   │   │   └── RenamingResult.java
│   │   │   ├── organization/
│   │   │   │   ├── Organizador.java                # Organize service
│   │   │   │   └── OrganizationResult.java
│   │   │   ├── extraction/
│   │   │   │   ├── Extrator.java                   # Extract service
│   │   │   │   └── ExtractionResult.java
│   │   │   ├── photoOrganization/
│   │   │   │   ├── PhotoOrganizator.java           # Photo org service
│   │   │   │   └── PhotoOrganizatorResult.java
│   │   │   └── duplicateFinder/
│   │   │       ├── DuplicateFinder.java            # Duplicate finder
│   │   │       └── DuplicateFinderResult.java
│   │   │
│   │   ├── model/
│   │   │   ├── Diretorio.java                      # Directory model
│   │   │   ├── FiltroExtensoes.java                # Extension filter
│   │   │   └── util/
│   │   │       └── FileUtils.java                  # File utilities
│   │   │
│   │   └── ui/                                     # Swing UI (legacy)
│   │       └── TelaPrincipal.java
│   │
│   ├── frontend/                                   # React + TypeScript
│   │   ├── src/
│   │   │   ├── main.tsx                            # Entry point
│   │   │   ├── App.tsx                             # Root component
│   │   │   │
│   │   │   ├── types/                              # TypeScript types
│   │   │   │   ├── index.ts                        # Barrel export
│   │   │   │   ├── api.types.ts                    # API types (200+ lines)
│   │   │   │   └── operations.types.ts             # Domain types (100+ lines)
│   │   │   │
│   │   │   ├── services/
│   │   │   │   └── api.ts                          # API client (200+ lines)
│   │   │   │
│   │   │   └── components/
│   │   │       ├── RenameOperation.tsx             # Rename UI
│   │   │       ├── OrganizeOperation.tsx           # Organize UI
│   │   │       ├── ExtractOperation.tsx            # Extract UI
│   │   │       ├── PhotoOrganizationOperation.tsx  # Photo UI
│   │   │       └── DuplicateFinderOperation.tsx    # Duplicate UI
│   │   │
│   │   ├── index.html                              # HTML template
│   │   ├── package.json                            # npm dependencies
│   │   ├── tsconfig.json                           # TypeScript config
│   │   ├── vite.config.ts                          # Vite config
│   │   ├── README.md                               # Frontend docs
│   │   └── TYPESCRIPT_ARCHITECTURE.md              # Architecture guide
│   │
│   └── resources/
│       ├── application.properties                  # Spring config
│       ├── exclusions.xml                          # Rename exclusions
│       ├── static/                                 # Built frontend (generated)
│       └── br/com/joaoborges/filemanager/resources/
│           ├── Extensoes.properties                # File extensions
│           └── GruposExtensoes.properties          # Extension groups
│
├── pom.xml                                         # Maven config
├── QUICKSTART.md                                   # Quick start guide
├── ARCHITECTURE.md                                 # This file
├── TYPESCRIPT_MIGRATION_SUMMARY.md                 # Migration summary
├── REACT_MIGRATION.md                              # Migration docs
└── CLAUDE.md                                       # Backend docs
```

## Technology Versions

| Component | Version | Purpose |
|-----------|---------|---------|
| Java | 21 | Backend runtime |
| Spring Boot | 3.5.8 | Web framework |
| React | 19.0.0 | UI library |
| TypeScript | 5.7.2 | Type system |
| Vite | 6.0.7 | Build tool |
| Material-UI | 6.3.2 | UI components |
| Node.js | 22.12.0 | JavaScript runtime |
| Maven | 3.x | Build tool |

## Key Features

### Type Safety
- ✅ 100% TypeScript coverage
- ✅ Strict mode enabled
- ✅ No `any` types (except necessary)
- ✅ Full API typing

### Performance
- ⚡ Hot reload < 100ms
- ⚡ Build time ~4 seconds
- ⚡ Bundle size 164 KB (gzipped)
- ⚡ Code splitting enabled

### Developer Experience
- 🎯 IntelliSense everywhere
- 🎯 Clear error messages
- 🎯 Comprehensive docs
- 🎯 Modern tooling

---

**Last Updated**: November 21, 2024
